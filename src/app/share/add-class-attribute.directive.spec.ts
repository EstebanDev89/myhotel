import { AddClassAttributeDirective } from './add-class-attribute.directive';

describe('AddClassAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
